export { default as Header } from "./Header";
export { default as AlbumShowcase } from "./AlbumShowcase";

// the need for the dark theme on the perez musik is not really needed but a must since it has become a request
